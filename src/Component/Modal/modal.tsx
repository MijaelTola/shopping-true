import React from 'react';
import { ModalBackground, ModalContent } from './modal.styled';

export const Modal = () => {
    return (
        <ModalBackground>
            <ModalContent>

            </ModalContent>
        </ModalBackground>
    )
}
